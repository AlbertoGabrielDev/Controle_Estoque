# Stock Module

Modulo de Estoque iniciado na Fase 3.

Status atual da etapa:
- Controller, FormRequest, Service, Model e Repositories migrados para o modulo.
- Rotas e paginas Vue de `Stock` co-localizadas no modulo.
- DB artifacts (`migrations`, `seeders`) migrados para `Database/*` no modulo.
- Testes smoke + fluxo minimo do modulo adicionados.
- Wrappers de compatibilidade permanecem em `App/*` e `resources/js/Pages/Stock/*`.

Proximas iteracoes:
- Refinar leituras complexas em repositorios/read-repositories conforme evolucao do modulo.
