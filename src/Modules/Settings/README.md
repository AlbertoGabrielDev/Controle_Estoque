# Settings Module

Modulo de Configuracoes iniciado na Fase 6.

Status inicial desta etapa:
- `SalesSettings`: Controller, Request, Service e Model migrados para o modulo.
- Rotas web e pagina Vue de `Settings/Sales` co-localizadas no modulo.
- DB artifacts de `AppSettings` (`migrations`) migrados para `Database/*`.
- Wrappers de compatibilidade permanecem em `App/*`, `database/*` e `resources/js/Pages/*`.
