# Taxes Module

Modulo de Taxas iniciado na Fase 5.

Status inicial desta etapa:
- `TaxRules`: Controller, Request, Service, Repository + RepositoryEloquent e Models migrados para o modulo.
- Rotas web e paginas Vue de `Taxes` co-localizadas no modulo.
- DB artifacts de `Taxes` (`migrations`, `seeders`) migrados para `Database/*`.
- Wrappers de compatibilidade permanecem em `App/*`, `database/*` e `resources/js/Pages/*`.
