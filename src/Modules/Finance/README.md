# Finance Module

Modulo Financeiro iniciado na Fase 5.

Status inicial desta etapa:
- `Centros de Custo`: Controller, Requests, Service, Repository e Model migrados para o modulo.
- `Contas Contabeis`: Controller, Requests, Service, Repository e Model migrados para o modulo.
- `Despesas`: Controller, Requests, Service, Repository e Model migrados para o modulo.
- Rotas web e paginas Vue de `CostCenters`, `AccountingAccounts` e `Expenses` co-localizadas no modulo.
- DB artifacts de `CentroCusto`, `ContaContabil` e `Despesa` (`migrations`, `seeders`) migrados para `Database/*`.
- Wrappers de compatibilidade permanecem em `App/*`, `database/*` e `resources/js/Pages/*`.
